/*
findCombinationsOfArray problem statement
For an array of n integers, find all possible combinations of length r
Assume r < n
Parameters: one array of numbers, one number that is lower than
length of array.
Later: Expand to different types of arrays.
I need to figure out how to declare a return of an array of arrays...
*/

//function findCombinationsOfArray(arr: number[], combLength: number): {
//    return arr;
//    }
